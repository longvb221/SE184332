<Window x:Class="LongVuBaoWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LongVuBaoWPF"
        mc:Ignorable="d"
        Title="LucySale-Dashboard" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <!-- Button Style with Hover Effect -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#FF5371BE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Width" Value="180"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <!-- Button Border with CornerRadius applied to Border -->
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Button.MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <ColorAnimation To="#6A4D9F" Duration="0:0:0.3" Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="Button.MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <ColorAnimation To="#FF5371BE" Duration="0:0:0.3" Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Define the columns for the layout -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <StackPanel Background="#FFC6DDD9" Grid.Column="0" VerticalAlignment="Stretch">
            <TextBlock Text="Lucy Sales" Foreground="#FF071A0D" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="20 30" />

            <Button Content="Customers" Name="BtnCustomer" Click="BtnCustomer_Click" Style="{StaticResource SidebarButtonStyle}" Background="#FF189B84"/>
            <Button Content="Products" Name="BtnProduct" Click="BtnProduct_Click" Style="{StaticResource SidebarButtonStyle}" Background="#FF189B84"/>
            <Button Content="Orders" Name="BtnOrder" Click="BtnOrder_Click" Style="{StaticResource SidebarButtonStyle}" Background="#FF189B84"/>
            <Button Content="Categories" Name="BtnCategory" Click="BtnCategory_Click" Style="{StaticResource SidebarButtonStyle}" Background="#FF189B84"/>
            <Button Content="Reports" Name="BtnReport" Click="BtnReport_Click" Style="{StaticResource SidebarButtonStyle}" Background="#FF189B84"/>
            <Button Content="Logout" Name="BtnLogout" Click="BtnLogout_Click" Foreground="#FF071A0D" Style="{StaticResource SidebarButtonStyle}" Background="#FF189B84" FontWeight="Bold"/>
        </StackPanel>

        <!-- Main content area -->
        <ContentControl x:Name="MainContent" Grid.Column="1" Margin="20,20,20,20" Background="#F9F9F9" />
    </Grid>
</Window>
