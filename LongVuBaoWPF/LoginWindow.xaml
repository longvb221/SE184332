<Window x:Class="LongVuBaoWPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LongVuBaoWPF"
        mc:Ignorable="d"
        Title="LoginWindow" Height="500" Width="500"
         WindowStartupLocation="CenterScreen"
 ResizeMode="NoResize"
 Background="#f2f2f2">
    <Border Margin="20" Padding="20" Background="#FFB6C2E0" CornerRadius="10" BorderBrush="#ccc" BorderThickness="1">
        <StackPanel Background="#FFC6DDD9">
            <TextBlock Text="Lucy Sales System" FontSize="20" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Center" Margin="0 0 0 20"/>

            <StackPanel Margin="0 0 0 10">
                <TextBlock Text="Role" FontWeight="SemiBold"/>
                <ComboBox x:Name="RoleComboBox" Height="30"/>
            </StackPanel>

            <StackPanel Margin="0 0 0 10">
                <TextBlock Text="Username / Phone" FontWeight="SemiBold"/>
                <TextBox x:Name="UsernameTextBox" Height="30" Padding="5" />
            </StackPanel>

            <StackPanel Margin="0 0 0 20">
                <TextBlock Text="Password (Employee only)" FontWeight="SemiBold"/>
                <PasswordBox x:Name="PasswordBox" Height="30" PasswordChanged="PasswordBox_PasswordChanged"/>
            </StackPanel>

            <Button Content="Login" Click="LoginButton_Click" Foreground="White"
        FontWeight="Bold" Height="35" BorderBrush="{x:Null}" Width="82">
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="Black"/>
                        <GradientStop Color="#FF9CC0C3" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

        </StackPanel>
    </Border>
</Window>
